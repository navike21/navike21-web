# API base URL — replace with your Vercel deployment domain
# Required by Client Components (browser-accessible)
NEXT_PUBLIC_API_URL=https://your-api-domain.vercel.app

# API base URL — server-side only, never exposed to the client bundle
API_URL=https://your-api-domain.vercel.app
