/* =========================
 * API Paths
 * Centralized route constants for all API endpoints.
 * Import this wherever you need to reference a backend path.
 * ========================= */

export const API_PATHS = {
  SUBSCRIBER_REGISTER: '/subscriber/register'
} as const
