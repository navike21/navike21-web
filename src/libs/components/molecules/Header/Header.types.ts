export type TStage = 'idle' | 'image' | 'menuItems' | 'closing' | 'closed'

export enum EStage {
  IDLE = 'idle',
  IMAGE = 'image',
  MENU_ITEMS = 'menuItems',
  CLOSING = 'closing',
  CLOSED = 'closed'
}
